#include <stdio.h>

//Purpose: Build a single array store 10 numbers and let user sort and print number in accending or decending order

int main()
{
	int iNumberInput[10] = {0};

	for (int x = 0; x < 10; x++)
	{
		printf("\nEnter number %d: ", x + 1);
		scanf_s("%d", &iNumberInput[x]);
	}

	do
	{

	} while ();
}